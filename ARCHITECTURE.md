# System Architecture & Flow Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT BROWSER                          │
│                    (Web Dashboard / API Client)                 │
└────────────┬────────────────────────────────────────────────────┘
             │
             │ HTTP/HTTPS Requests
             │
             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        EXPRESS SERVER                           │
│                       (Port 3000)                               │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   MIDDLEWARE LAYER                        │  │
│  │  • CORS                                                   │  │
│  │  • Body Parser (JSON/URLEncoded)                         │  │
│  │  • Rate Limiter (10 req/min for shorten)                │  │
│  │  • Static File Server (public/)                         │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    ROUTING LAYER                         │  │
│  │                                                          │  │
│  │  /api/*           →  API Routes                         │  │
│  │  /:shortCode      →  Redirect Routes                    │  │
│  │  /                →  Static Dashboard                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  BUSINESS LOGIC                          │  │
│  │                                                          │  │
│  │  • Short Code Generator (nanoid + collision check)      │  │
│  │  • URL Validator                                        │  │
│  │  • Analytics Tracker (IP, GeoLocation, User Agent)     │  │
│  │  • QR Code Generator                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────┬────────────────────────────────────────────────────┘
             │
             │ Mongoose ODM
             │
             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        MONGODB DATABASE                         │
│                   (localhost:27017)                             │
│                                                                 │
│  ┌──────────────────┐         ┌──────────────────┐            │
│  │   urls           │         │   clicks         │            │
│  │  Collection      │         │  Collection      │            │
│  │                  │         │                  │            │
│  │  • originalUrl   │         │  • shortCode     │            │
│  │  • shortCode ⚡   │         │  • timestamp ⚡   │            │
│  │  • customAlias ⚡ │         │  • ipAddress     │            │
│  │  • clicks        │         │  • userAgent     │            │
│  │  • expiresAt ⚡   │         │  • country       │            │
│  │  • isActive      │         │  • city          │            │
│  │  • createdAt     │         │  • visitorId ⚡   │            │
│  │                  │         │  • referrer      │            │
│  └──────────────────┘         └──────────────────┘            │
│      ⚡ = Indexed                  ⚡ = Indexed                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 URL Shortening Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Submits long URL
     │    (+ optional custom alias)
     │    (+ optional expiration)
     ▼
┌─────────────────┐
│  Frontend Form  │
└────┬────────────┘
     │
     │ 2. POST /api/shorten
     │    Content-Type: application/json
     │    { originalUrl, customAlias?, expiresAt? }
     ▼
┌────────────────────────┐
│  Rate Limiter Check    │
│  (10 req/min per IP)   │
└────┬───────────────────┘
     │
     │ ✅ Pass
     ▼
┌────────────────────────┐
│  Input Validation      │
│  • Valid URL format?   │
│  • Valid alias format? │
│  • Valid date?         │
└────┬───────────────────┘
     │
     │ ✅ Valid
     ▼
┌────────────────────────┐    NO   ┌──────────────────┐
│  Custom Alias Given?   ├────────►│ Generate Random  │
└────┬───────────────────┘         │ Short Code       │
     │ YES                          │ (nanoid)         │
     ▼                              └────┬─────────────┘
┌────────────────────────┐              │
│  Check Alias Available │              │
└────┬───────────────────┘              │
     │                                   │
     │ ✅ Available         ◄────────────┘
     ▼
┌────────────────────────┐    ❌ Collision
│  Check for Collision   ├─────────────────┐
└────┬───────────────────┘                 │
     │ ✅ No Collision                     │
     │                                     │
     ▼                                     │
┌────────────────────────┐                 │
│  Save to Database      │                 │
│  • Url document        │◄────────────────┤
│  • Set createdAt       │      Retry (max 5×)
│  • Set expiresAt       │
└────┬───────────────────┘
     │
     │ 3. Return Success
     ▼
┌────────────────────────┐
│  Response              │
│  {                     │
│    success: true,      │
│    data: {             │
│      shortUrl,         │
│      shortCode,        │
│      ...               │
│    }                   │
│  }                     │
└────┬───────────────────┘
     │
     ▼
┌────────────────────────┐
│  Display Short URL     │
│  • Show in UI          │
│  • Enable Copy button  │
│  • Enable QR button    │
└────────────────────────┘
```

---

## 🔀 URL Redirect & Analytics Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Clicks or visits short URL
     │    http://localhost:3000/abc123
     ▼
┌────────────────────────┐
│  GET /:shortCode       │
│  Express Route Handler │
└────┬───────────────────┘
     │
     │ 2. Extract shortCode
     ▼
┌────────────────────────┐
│  Query Database        │
│  findOne({             │
│    shortCode: 'abc123',│
│    isActive: true      │
│  })                    │
└────┬───────────────────┘
     │
     ├──► NOT FOUND ────────────┐
     │                          ▼
     │                    ┌──────────────┐
     │                    │  Return 404  │
     │                    │  Show 404    │
     │                    │  HTML page   │
     │                    └──────────────┘
     │
     ├──► EXPIRED ──────────────┐
     │                          ▼
     │                    ┌──────────────┐
     │                    │  Return 410  │
     │                    │  Show expired│
     │                    │  HTML page   │
     │                    └──────────────┘
     │
     │ ✅ FOUND & ACTIVE
     ▼
┌────────────────────────────────┐
│  Extract Visitor Information   │
│  • IP Address                  │
│  • User Agent                  │
│  • Referrer                    │
│  • Timestamp                   │
└────┬───────────────────────────┘
     │
     │ 3. Generate Visitor ID
     │    SHA256(IP + UserAgent)
     ▼
┌────────────────────────────────┐
│  GeoIP Lookup                  │
│  • Country from IP             │
│  • City from IP                │
└────┬───────────────────────────┘
     │
     │ 4. Save Analytics (async/non-blocking)
     ▼
┌────────────────────────────────┐
│  Create Click Document         │
│  {                             │
│    shortCode,                  │
│    timestamp,                  │
│    ipAddress,                  │
│    userAgent,                  │
│    country,                    │
│    city,                       │
│    visitorId,                  │
│    referrer                    │
│  }                             │
└────┬───────────────────────────┘
     │
     │ 5. Increment URL click count
     ▼
┌────────────────────────────────┐
│  Update Url Document           │
│  { $inc: { clicks: 1 } }       │
└────┬───────────────────────────┘
     │
     │ 6. Redirect (doesn't wait for saves)
     ▼
┌────────────────────────────────┐
│  res.redirect(originalUrl)     │
│  302 Found                     │
└────┬───────────────────────────┘
     │
     ▼
┌────────────────────────────────┐
│  User Lands on Original URL    │
└────────────────────────────────┘
```

---

## 📊 Analytics Query Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Clicks "Analytics" button
     │    for a short link
     ▼
┌────────────────────────────────┐
│  Frontend JavaScript           │
│  showAnalyticsModal(shortCode) │
└────┬───────────────────────────┘
     │
     │ 2. GET /api/analytics/:shortCode?days=7
     ▼
┌────────────────────────────────┐
│  API Route Handler             │
└────┬───────────────────────────┘
     │
     │ 3. Find URL by shortCode
     ▼
┌────────────────────────────────┐
│  Query urls Collection         │
│  findOne({ shortCode })        │
└────┬───────────────────────────┘
     │
     │ ✅ Found
     │ 4. Calculate date range
     ▼
┌────────────────────────────────┐
│  daysAgo = now - 7 days        │
└────┬───────────────────────────┘
     │
     │ 5. Query all clicks in range
     ▼
┌────────────────────────────────┐
│  Query clicks Collection       │
│  find({                        │
│    shortCode,                  │
│    timestamp: { $gte: daysAgo }│
│  })                            │
└────┬───────────────────────────┘
     │
     │ 6. Process data
     ▼
┌────────────────────────────────┐
│  Calculate Metrics             │
│                                │
│  • Total clicks = url.clicks   │
│  • Unique visitors =           │
│    Set(visitorId).size         │
│  • Clicks by date =            │
│    Group by date, count        │
│  • Clicks by country =         │
│    Group by country, count     │
│  • Recent clicks =             │
│    Sort by timestamp desc,     │
│    Take first 10               │
└────┬───────────────────────────┘
     │
     │ 7. Format response
     ▼
┌────────────────────────────────┐
│  Return JSON                   │
│  {                             │
│    success: true,              │
│    data: {                     │
│      url: { ... },             │
│      analytics: {              │
│        totalClicks,            │
│        uniqueVisitors,         │
│        clicksByDate,           │
│        clicksByCountry,        │
│        recentClicks            │
│      }                         │
│    }                           │
│  }                             │
└────┬───────────────────────────┘
     │
     │ 8. Display in modal
     ▼
┌────────────────────────────────┐
│  Frontend Renders Analytics    │
│  • Total/Unique stats          │
│  • Country breakdown chart     │
│  • Recent activity list        │
└────────────────────────────────┘
```

---

## 🔐 Rate Limiting Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ Makes HTTP Request
     ▼
┌────────────────────────────────┐
│  Express Rate Limiter          │
│  Middleware                    │
└────┬───────────────────────────┘
     │
     │ 1. Extract IP Address
     ▼
┌────────────────────────────────┐
│  Get Client IP                 │
│  • x-forwarded-for header      │
│  • x-real-ip header            │
│  • req.connection.remoteAddress│
└────┬───────────────────────────┘
     │
     │ 2. Check in-memory store
     ▼
┌────────────────────────────────┐
│  Rate Limit Store              │
│  {                             │
│    "192.168.1.1": {            │
│      count: 5,                 │
│      resetTime: timestamp      │
│    }                           │
│  }                             │
└────┬───────────────────────────┘
     │
     ├──► Count < Max (10) ───────┐
     │                            │
     │                            ▼
     │                      ┌──────────────┐
     │                      │  Allow       │
     │                      │  Increment   │
     │                      │  Pass to     │
     │                      │  next()      │
     │                      └──────────────┘
     │
     └──► Count >= Max ──────────┐
                                 │
                                 ▼
                           ┌──────────────┐
                           │  Block       │
                           │  Return 429  │
                           │  {           │
                           │    error,    │
                           │    retryAfter│
                           │  }           │
                           └──────────────┘
```

---

## 🎨 Frontend Component Flow

```
┌────────────────────────────────────────────┐
│            index.html (Dashboard)          │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │       Shorten URL Form               │ │
│  │  • Long URL input                    │ │
│  │  • Custom alias input (optional)     │ │
│  │  • Expiration date picker (optional) │ │
│  │  • Submit button                     │ │
│  └──────┬───────────────────────────────┘ │
│         │                                  │
│         │ On Submit                        │
│         ▼                                  │
│  ┌──────────────────────────────────────┐ │
│  │       Result Display                 │ │
│  │  • Short URL (readonly input)        │ │
│  │  • Copy button                       │ │
│  │  • Generate QR button                │ │
│  │  • View Analytics button             │ │
│  │  • QR code image (hidden initially)  │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │       URL List Section               │ │
│  │  • Refresh button                    │ │
│  │  • List of URL items                 │ │
│  │    - Short URL                       │ │
│  │    - Original URL                    │ │
│  │    - Click stats                     │ │
│  │    - Status badge (Active/Expired)   │ │
│  │    - Action buttons                  │ │
│  │      • Copy                          │ │
│  │      • Analytics                     │ │
│  │      • QR Code                       │ │
│  │      • Delete                        │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │       Analytics Modal (hidden)       │ │
│  │  • URL information                   │ │
│  │  • Total clicks card                 │ │
│  │  • Unique visitors card              │ │
│  │  • Clicks by country list            │ │
│  │  • Recent clicks list                │ │
│  │  • Close button                      │ │
│  └──────────────────────────────────────┘ │
└────────────────────────────────────────────┘
```

---

## 📦 Data Models & Relationships

```
┌─────────────────────────────────────┐
│           Url Model                 │
├─────────────────────────────────────┤
│ _id: ObjectId                       │
│ originalUrl: String ✓               │
│ shortCode: String ✓ 🔑 (unique)     │
│ customAlias: String 🔑 (unique)     │
│ clicks: Number (default: 0)         │
│ expiresAt: Date ⏰                   │
│ isActive: Boolean (default: true)   │
│ createdBy: String (IP)              │
│ createdAt: Date ⏰                   │
│ updatedAt: Date ⏰                   │
└────────┬────────────────────────────┘
         │
         │ One-to-Many
         │ (1 URL has many clicks)
         │
         ▼
┌─────────────────────────────────────┐
│          Click Model                │
├─────────────────────────────────────┤
│ _id: ObjectId                       │
│ shortCode: String ✓ 🔑 (indexed)    │
│ timestamp: Date ✓ ⏰ (indexed)       │
│ ipAddress: String ✓                 │
│ userAgent: String                   │
│ referrer: String                    │
│ country: String (from GeoIP)        │
│ city: String (from GeoIP)           │
│ visitorId: String 🔑 (indexed)      │
└─────────────────────────────────────┘

Legend:
✓ = Required field
🔑 = Indexed field
⏰ = Timestamp field
```

---

## 🔍 Key Indexes Explained

```
urls Collection:
├─ shortCode: Unique Index
│  └─ Fast lookups when redirecting
│  └─ Prevents duplicate codes
│
├─ customAlias: Unique Sparse Index
│  └─ Fast lookups for custom aliases
│  └─ Sparse = only indexes non-null values
│
└─ expiresAt: TTL Index
   └─ Automatically removes expired docs
   └─ MongoDB handles cleanup

clicks Collection:
├─ shortCode + timestamp: Compound Index
│  └─ Optimizes analytics queries by date range
│
└─ shortCode + visitorId: Compound Index
   └─ Fast unique visitor counting
```

---

This diagram documentation provides a comprehensive visual understanding of how the entire system works!
